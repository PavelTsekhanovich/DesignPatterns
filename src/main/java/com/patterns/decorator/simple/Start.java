package com.patterns.decorator.simple;

import com.patterns.decorator.simple.decorators.BorderDecorator;
import com.patterns.decorator.simple.decorators.ColorDecorator;
import com.patterns.decorator.simple.objects.Component;
import com.patterns.decorator.simple.objects.TextView;
import com.patterns.decorator.simple.objects.Window;

public class Start {

    public static void main(String[] args){

        /*Component windowWithBorder = new ColorDecorator(new BorderDecorator(new Window()));
        windowWithBorder.draw();

        Component textVies = new ColorDecorator(new BorderDecorator(new TextView()));
        textVies.draw();*/
    }
}
